---
import PortalLayout from '../../../layouts/PortalLayout.astro'
import { PortalContactForm } from '../../../components/portal/PortalContactForm'
import { getPartner } from '../../../lib/portal-api'

export const prerender = false

const partner = getPartner()
---

<PortalLayout title="Contact — Espace Partenaire Semisto" activePage="contact">
  <div class="max-w-2xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
    <PortalContactForm
      client:load
      partnerName={partner.name}
      onBack={() => { window.location.href = '/portal/' }}
    />
  </div>
</PortalLayout>
